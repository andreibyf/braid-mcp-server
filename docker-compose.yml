version: "3.9"

services:
  braid-mcp-server:
    build: .
    container_name: braid-mcp-server
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    ports:
      - "8000:8000"
